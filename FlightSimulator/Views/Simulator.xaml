<Window x:Class="FlightSimulator.Views.Simulator"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:c="clr-namespace:FlightSimulator.Views"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:m="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        xmlns:local="clr-namespace:FlightSimulator"
        mc:Ignorable="d"
        Title="Flight Simulator" Height="500" Width="1000" Background="LightBlue">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <c:Control Grid.Column="1" Grid.Row="1" Height="240"/>
        <c:Map Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" x:Name="myMap"/>
        <c:Dashboard Grid.Row="0" Grid.Column="1" x:Name="dash"/>
        <Button Content="Disconnect" Grid.Column="1" HorizontalAlignment="Left" Margin="238,3.667,0,0" VerticalAlignment="Top" Width="85" FontWeight="Bold" FontFamily="Segoe Print" FontSize="14" Click="Button_Click" Grid.Row="1"/>
    </Grid>
</Window>
